使用git init初始化一个git仓库
添加文件到git仓库：（1）使用git add;（2）使用 git commit -m "文件说明"

用git status 命令掌握工作区的状态
可以用git diff查看修改内容

git reset --hard commit_id
git log可以查看提交历史，确定回退的版本
git reflog 可以查看命令历史，确定前进的版本

renew
